// Answers the generated file's `#include "daisy_seed.h"` on the host.
//
// Named to match libDaisy's real header so the generated source compiles
// untouched — a parity harness that rewrites the code it is testing is
// testing something else. The contents are the shim; see dp_host_daisy.h.
#pragma once
#include "dp_host_daisy.h"
